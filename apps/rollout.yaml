apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ml-inference-rollout
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/sammaher1/argo-ml-workloads.git
    targetRevision: main
    path: workloads/rollouts
    directory:
      recurse: false
  destination:
    server: https://kubernetes.default.svc
    namespace: ml-lab-rollout
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
